/// An abstract class representing the interface for a view model that handles albums.
abstract class IAlbumViewModel {
  /// Creates a const instance of [IAlbumViewModel].
  const IAlbumViewModel();

  // Add your abstract methods or properties here
}
